<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper의 namespace속성은 연결할 mapper의 interface 풀 경로로 작성  -->
<mapper namespace="egovframework.ddan.mapper.PointMapper">


   <insert id="insertList" >
       insert into point (gid, lat, lon, date, time, andoid_id) values 
       (nextval('gid_sequence'), #{latitude}, #{longitude}, CURRENT_DATE, CURRENT_TIME, #{andoid_id})
   </insert>

	

</mapper>